//usuarios.js 
    // (id, nome, whats, email, cep) - 3 usuários - html(sites)
    // função buscarUsuario(id)

//pedidos.js
    // (id, usuarioId, produtoId, quantidade, valor) - 3 pedidos - html(sites)
    // função buscarPedido(usuarioId)

//produtos.js
    // (id, nome, preco) - 5 produtos
    // função buscarProdutos()

//index.js
// importar os módulos
// fechar a conta de cada um dos clientes = função fecharConta(usuarioId)
// criar arquivo (a conta fechada) 